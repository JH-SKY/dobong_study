# 📝 Learning Record | 2026-02-23 (W08-01)

---

## 1. 📋 학습 요약
실무 수준의 **로깅(Logging) 포맷** 설정법을 익히고, DB 형상 관리를 위한 **알렘빅(Alembic)**의 필요성과 프로젝트 보안 및 청결을 위한 **.gitignore** 규칙을 체계적으로 학습함.
유지보수성을 높이는 **커스텀 예외 처리(Custom Exception)** 및 효율적인 개발 도구인 **커서(Cursor)** 활용법을 통합적으로 학습

---

## 2. 📚 배운 개념 정리

### 🪵 로깅 (Logging) & 포맷터 (Formatter)
* **개념**: 프로그램의 모든 활동을 기록하는 '블랙박스'와 같음.
* **주요 속성**:
    * `%(asctime)s`: 로그 발생 시간
    * `%(levelname)s`: 로그 레벨 (INFO, WARNING, ERROR 등)
    * `%(filename)s:%(lineno)d`: **(핵심)** 에러가 발생한 파일명과 줄 번호
* **이유**: 일기를 쓸 때 '언제, 어디서'를 적어야 나중에 기억이 나듯, 코드에도 위치 정보를 남겨야 에러를 바로 검거(?)할 수 있음.

### ⚗️ 알렘빅 (Alembic)
* **개념**: 데이터베이스 설계도(Schema)의 버전 관리 도구.
* **이유**: 서비스 운영 중 DB 구조를 바꿀 때 기존 데이터를 보존하면서 안전하게 업데이트(마이그레이션)하기 위해 사용함.

### 🚫 .gitignore
* **개념**: Git이 추적하지 말아야 할 파일 목록을 정의.
* **대상**: 가상환경(`.venv`), 환경변수(`.env`), 로그 파일(`*.log`), OS 임시 파일 등.
* **이유**: 보안이 중요한 정보나 용량이 큰 불필요한 파일을 공유하지 않기 위함.

### ⚠️ 커스텀 예외 처리 (Custom Exception)
* **개념**: `HTTPException`을 직접 쓰지 않고, 이를 상속받은 클래스(예: `NotFoundException`)를 만들어 사용하는 것.
* **이유**: 단순히 타이핑을 줄이는 것이 아니라, 에러 발생 시 **일관된 응답 규격**을 유지하고 **유지보수 지점**을 한곳으로 모으기 위함(클래스 활용의 핵심 목적과 동일).

### 🤖 커서 (Cursor) AI 에디터 활용
* **코드 설명**: 드래그 후 `Alt + Enter`(Quick Question)를 통해 AI에게 코드의 의도나 로직 설명을 즉시 들을 수 있음.
* **Chat 활용**: `Ctrl + L`로 코드를 보내 파일 간의 관계나 복잡한 설계를 심도 있게 질문 가능.

---

## 3. 💻 코드리뷰

### 🛠 로깅 설정 및 실무 적용
```python
import logging

# [1] 포맷터 설정: 실무에서 가장 선호하는 [파일명:줄번호] 조합
# 나중에 에러 로그만 보고도 해당 코드로 즉시 이동할 수 있게 설계함
formatter = logging.Formatter(
    fmt="%(asctime)s %(levelname)-8s [%(filename)s:%(lineno)d] %(name)s - %(message)s",
    datefmt="%Y-%m-%d %H:%M:%S",
)

# [2] 서비스 레이어 적용 (AuthService 예시)
logger = logging.getLogger(__name__)

class AuthService:
    def signup(self, email):
        # 정보 기록: 누가 가입을 시도했는지 남겨서 운영 데이터로 활용
        logger.info(f"회원가입 시도: {email}")
        
        if user_exists:
            # 경고 기록: 에러는 아니지만 중복 시도와 같은 특이사항은 warning으로 남김
            logger.warning(f"회원가입 실패 - 이메일 중복: {email}")


### 🛠 커스텀 예외 및 로깅 적용
import logging
from fastapi import HTTPException, status

# [1] 실무형 로깅 설정
formatter = logging.Formatter(
    # 파일명과 줄번호를 포함하여 에러 위치 추적을 용이하게 함
    fmt="%(asctime)s %(levelname)-8s [%(filename)s:%(lineno)d] %(name)s - %(message)s",
    datefmt="%Y-%m-%d %H:%M:%S",
)

# [2] 커스텀 예외 클래스 정의 (유지보수와 일관성을 위함)
class NotFoundException(HTTPException):
    def __init__(self, detail: str):
        # 상속을 통해 부모(HTTPException)의 기능을 쓰면서 설정만 고정함
        super().__init__(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=detail
        )

# [3] 서비스 레이어 적용
logger = logging.getLogger(__name__)

def get_post(post_id):
    if not post:
        logger.warning(f"게시글 조회 실패: id={post_id}")
        # 라우터 수정 없이 예외만 던지면 시스템이 알아서 처리함
        raise NotFoundException("존재하지 않는 게시글입니다.")
```

---

## 4. ❓ 헷갈렸던 점

> **Q. 에러가 나면 터미널에 자동으로 뜨는데 왜 직접 코드를 짜서 로그를 남겨야 할까?**

* **사고 과정**: "터미널(Traceback)만 보면 되는 거 아닌가?" → "아, 서버는 내가 보고 있을 때만 돌아가는 게 아니지!"
* **해결**: 터미널 에러는 프로그램이 죽을 때 내뱉는 **'일회성 비명'**임. 반면, 로깅은 서버가 24시간 동안 무슨 일을 했는지 기록하는 **'항구적인 일지'**임. 특히 사용자가 "어제 새벽에 안 됐어요"라고 문의했을 때 로그 파일이 없으면 원인 파악이 불가능함.

> **Q. 커스텀 예외를 만들면 라우터 코드도 다 바꿔야 할까?**

* **의문점**: `HTTPException`을 클래스로 감싸서(Custom) 던지면 배달 역할인 라우터에서도 처리 방식이 바뀌어야 하는지 궁금했음.
* **정리**: 
    1. **분업의 원리**: 라우터는 서비스가 던진 예외를 그대로 통과시켜 시스템(FastAPI)에 전달할 뿐이므로 **코드를 수정할 필요가 없음**.
    2. **추상화의 장점**: 서비스 로직에서 예외 이름을 구체적으로 지어주면(`NotFoundException`), 코드를 읽는 사람(나 혹은 동료)이 404 같은 숫자보다 훨씬 빠르게 상황을 이해할 수 있음.

---

## 5. 🚀 실무 관점 (Insight)

* **디버깅 효율**: `%(filename)s:%(lineno)d` 설정 하나가 디버깅 시간을 **수 시간에서 수 분으로** 단축함.
* **보안 철칙**: `.gitignore`에 로그 파일과 환경 변수를 누락하는 것은 실무에서 **치명적인 보안 결함**으로 간주됨. 항상 프로젝트 시작 시 최우선으로 세팅해야 함.
* **데이터 보존**: 알렘빅을 사용하지 않고 DB를 직접 수정하는 행위는 실무에서 매우 위험함. 모든 DB 변경은 '마이그레이션 파일'을 통해 기록으로 남겨야 함.
* **기술 부채 방지**: 처음부터 커스텀 예외를 정의해두면, 나중에 에러 메시지 형식을 일괄 변경해야 할 때 수백 개의 파일을 뒤지지 않고 클래스 파일 하나만 고치면 됨.
* **사용자 가치**: 예외 처리를 꼼꼼히 하는 것은 결국 사용자에게 '알 수 없는 에러' 대신 '친절한 안내'를 제공하여 서비스 이탈을 막는 핵심 작업임.
---

## 🏃 성장 포인트
* 단순히 코드가 돌아가는 것을 넘어, **'운영 가능하고 추적 가능한'** 코드 설계의 중요성을 깨달음.
* `.gitignore` 작성 시 와일드카드(`*`) 활용법과 OS별 임시 파일 특성을 이해함.
* 클래스의 상속 개념을 예외 처리에 대입하며 **'유지보수 가능한 코드'**에 대한 감을 잡음.
* 단순히 에러를 안 나게 하는 것을 넘어, 에러가 났을 때의 **추적(Logging)**과 **대처(Exception Handling)**가 실무의 핵심임을 깨달음.